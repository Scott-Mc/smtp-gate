# /etc/smtp-gate/config -- sourced by smtp-gate

# Linux bridge that VM tap interfaces are enslaved to.
BRIDGE="br0"

# Outbound TCP ports to block (comma-separated).
BLOCKED_PORTS="25,465"

# Rate-limited logging of dropped packets.  Set to 0 to disable.
LOG_DROPS="1"
LOG_RATE="5/second"
LOG_BURST="50"

# Override the regex used to identify uplink (physical) ports on the bridge.
# Default matches ens*, eno*, enp*, eth*, bond*.
# UPLINK_REGEX="^(ens|eno|enp|eth|bond)[0-9a-zA-Z_.:-]*$"
